import BatteryTimeRemainingWidgetCore
import Foundation

struct BatteryUsageSummaryProvider: Sendable {
    func currentSummary() -> BatteryUsageSummary {
        // Replace these placeholders with privacy-safe aggregates from your app.
        // Examples: app-level transfer counters, user activity sessions, or values
        // persisted by a background-capable containing app process.
        BatteryUsageSummary.placeholder
    }
}
